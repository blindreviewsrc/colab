from setuptools import find_packages, setup
setup(
    name='quizlib',
    packages=find_packages(include=['quizlib']),
    version='0.1.0',
    description='quiz library',
    author='Blind',
    license='MIT',
    install_requires=[]
)
